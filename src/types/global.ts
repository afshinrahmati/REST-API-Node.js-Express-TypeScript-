export enum ResultStatusTypes{
    Success = "SUCCESS",
    Failed = "FAILED",
    Progress = "PROGRESS",
    Canceled = "CANCELED",
}

export enum ActivationStatus {
    Inactive = "INACTIVE",
    Active = "ACTIVE",
    Deleted = "DELETED",
}

export enum RequestMethod {
    get = "GET",
    post = "POST",
    put = "PUT",
    patch = "PATCH",
    delete = "DELETE",
    options = "OPTIONS",
}